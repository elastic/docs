This directory contains integration tests that can be run with
`./build_docs --self-test`. While you can run rspec locally with bundler it
isn't likely to work outside of the docker image booted by `--self-test`.
